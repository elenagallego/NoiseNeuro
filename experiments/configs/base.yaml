seed: 42
device: "cuda"

data:
  data_root: "./data/eeg_sample"
  window_size: 256
  window_stride: 128
  normalize: true
  batch_size: 64
  num_workers: 4
  val_split: 0.1
  test_split: 0.1

train:
  epochs: 50
  lr: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 10
