include: base.yaml

model:
  type: "noise_controller_snn"
  n_channels: 22
  window_size: 256
  latent_dim: 64
  hidden_dims: [128, 64]

noise:
  enabled: true
  where: "input"
  sigma: 0.1

train:
  epochs: 200
  lr: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 20
