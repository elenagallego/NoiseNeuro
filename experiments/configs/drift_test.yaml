include: base.yaml

data:
  split_mode: "session_split"

model:
  type: "ann_autoencoder"
  n_channels: 59
  window_size: 256
  latent_dim: 64
  hidden_dims: [128, 64]

noise:
  enabled: false
  sigma: 0.0
